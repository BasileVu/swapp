export class Note {
    offer: number;
    text: string;
    note: number;

    constructor(offer: number, text: string, note: number) {
        this.offer = offer;
        this.text = text;
        this.note = note;
    }
}