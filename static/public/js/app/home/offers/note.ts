export class Note {
    offer: number;
    text: string;
    note: number;

    constructor(offer: number, note: number) {
        this.offer = offer;
        this.text = "";
        this.note = note;
    }
}